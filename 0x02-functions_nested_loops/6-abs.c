#include "main.h"

/**
 * _abs - computes absolute values
 * @n: the integer to be inputed
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);

}
