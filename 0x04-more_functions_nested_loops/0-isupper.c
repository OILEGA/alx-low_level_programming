#include "main.h"

/**
 * _isupper - checks if parameter is an uppercase character.
 * 0c: input character
 * Return: 1 if it is an uppercase, 0 if otherwise
*/
int _isupper(int c);

{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
